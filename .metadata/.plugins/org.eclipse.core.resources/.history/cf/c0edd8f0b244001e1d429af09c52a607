package com.msb.lrg.ds.dp;

public class SubsetSumRecursive {

	public static void main(String[] args) {
		int[] arr = new int[] {10,5,2,6,3};
		int sum = 8;
		System.out.println("O/P : " + SubsetSumRecursive.solve(arr, sum, arr.length));
	}
	
	public static int solve(int[] arr, int sum, int n) {
		
		if(n == 0 || sum < 0)
			return 0;
		
		if(sum == 0)
			return 1;
		
		return Math.max( 1 + solve(arr, sum - arr[n-1], n-1),
							solve(arr, sum, n-1));
		
	}

}
